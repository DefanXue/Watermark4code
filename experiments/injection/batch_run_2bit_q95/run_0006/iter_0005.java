public boolean checkRepeating(String str) {  // Check for null values
  if (str == null || str.isEmpty()) {
    return false;
 } else {
 for (char c : str.toCharArray()) {
 String tmp = "" + c + "<event>";
      }
      return str.matches("\\d+|\\d+-\\d+");
  }
}